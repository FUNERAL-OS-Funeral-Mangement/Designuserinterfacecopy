
  # Design User Interface (Copy)

  This is a code bundle for Design User Interface (Copy). The original project is available at https://www.figma.com/design/sps8o1AZfG37sUEDUN0Xrz/Design-User-Interface--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  